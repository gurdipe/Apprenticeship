<%= form_for(@how_to_apply) do |f| %>
  <% if @how_to_apply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_to_apply.errors.count, "error") %> prohibited this how_to_apply from being saved:</h2>

      <ul>
      <% @how_to_apply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
